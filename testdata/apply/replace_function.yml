manage_routine: true
init: |
  CREATE FUNCTION public.f() RETURNS integer LANGUAGE sql AS $$ SELECT 1 $$;
desired: |
  CREATE FUNCTION public.f() RETURNS integer LANGUAGE sql STABLE AS $$ SELECT 2 $$;
applied_sql: |
  CREATE OR REPLACE FUNCTION public.f()
      RETURNS integer
      LANGUAGE sql
      STABLE
      AS $$ SELECT 2 $$;
applied: |
  -- public.f()
  CREATE OR REPLACE FUNCTION public.f()
      RETURNS integer
      LANGUAGE sql
      STABLE
      AS $$ SELECT 2 $$;
