bulk_alter: true
verify_no_drift: true
init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE TABLE public.orders (
      id integer NOT NULL,
      CONSTRAINT orders_pkey PRIMARY KEY (id)
  );
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE TABLE public.orders (
      id integer NOT NULL,
      user_id integer,
      total numeric(10,2),
      CONSTRAINT orders_pkey PRIMARY KEY (id),
      CONSTRAINT orders_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
  );
applied: |
  -- public.orders
  CREATE TABLE public.orders (
      id integer NOT NULL,
      user_id integer,
      total numeric(10,2),
      CONSTRAINT orders_pkey PRIMARY KEY (id)
  );

  ALTER TABLE ONLY public.orders ADD CONSTRAINT orders_user_id_fkey FOREIGN KEY (user_id) REFERENCES users(id);

  -- public.users
  CREATE TABLE public.users (
      id integer NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
applied_sql: |
  ALTER TABLE public.orders
    ADD COLUMN user_id integer,
    ADD COLUMN total numeric(10,2);
  ALTER TABLE ONLY public.orders ADD CONSTRAINT orders_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users (id);
