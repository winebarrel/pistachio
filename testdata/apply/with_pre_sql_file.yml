# The pre-SQL creates a table the desired schema does not describe, so the
# post-apply re-plan would want to drop it. The drift is the point of the case.
skip_drift_check: true
pre_sql_file: |
  CREATE TABLE public.pre_hook (
      id integer NOT NULL,
      CONSTRAINT pre_hook_pkey PRIMARY KEY (id)
  );
init: ""
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
applied_sql: |
  CREATE TABLE public.pre_hook (
      id integer NOT NULL,
      CONSTRAINT pre_hook_pkey PRIMARY KEY (id)
  );

  CREATE TABLE public.users (
      id integer NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
applied: |
  -- public.pre_hook
  CREATE TABLE public.pre_hook (
      id integer NOT NULL,
      CONSTRAINT pre_hook_pkey PRIMARY KEY (id)
  );

  -- public.users
  CREATE TABLE public.users (
      id integer NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
