init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      name text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      name text COLLATE "C" NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
applied: |
  -- public.users
  CREATE TABLE public.users (
      id integer NOT NULL,
      name text COLLATE "C" NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
