init: |
  CREATE TABLE public.items (
      name text NOT NULL
  );
desired: |
  CREATE TABLE public.items (
      name text NOT NULL,
      id bigint GENERATED ALWAYS AS IDENTITY (START WITH 50 INCREMENT BY 2)
  );
applied: |
  -- public.items
  CREATE TABLE public.items (
      name text NOT NULL,
      id bigint GENERATED ALWAYS AS IDENTITY (START WITH 50 INCREMENT BY 2)
  );
