init: ""
desired: |
  CREATE TABLE public.users (
      id integer PRIMARY KEY
  );
applied: |
  -- public.users
  CREATE TABLE public.users (
      id integer NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
