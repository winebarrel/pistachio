init: |
  CREATE TABLE public.events (
      id bigint NOT NULL,
      starts_at time without time zone NOT NULL,
      vacation_hours integer NOT NULL,
      CONSTRAINT events_pkey PRIMARY KEY (id)
  );
  ALTER TABLE public.events ENABLE ROW LEVEL SECURITY;
  CREATE POLICY events_visible ON public.events FOR SELECT USING ((starts_at >= '00:00:00'::time without time zone) AND (vacation_hours >= '-40'::integer));
desired: |
  CREATE TABLE public.events (
      id bigint NOT NULL,
      starts_at time NOT NULL,
      vacation_hours integer NOT NULL,
      CONSTRAINT events_pkey PRIMARY KEY (id)
  );
  ALTER TABLE public.events ENABLE ROW LEVEL SECURITY;
  CREATE POLICY events_visible ON public.events FOR SELECT USING (starts_at >= '00:00:00' AND vacation_hours >= -40);
applied: |
  -- public.events
  CREATE TABLE public.events (
      id bigint NOT NULL,
      starts_at time without time zone NOT NULL,
      vacation_hours integer NOT NULL,
      CONSTRAINT events_pkey PRIMARY KEY (id)
  );

  ALTER TABLE public.events ENABLE ROW LEVEL SECURITY;
  CREATE POLICY events_visible ON public.events FOR SELECT USING (((starts_at >= '00:00:00'::time without time zone) AND (vacation_hours >= '-40'::integer)));
applied_sql: ""
verify_no_drift: true
