init: |
  CREATE TABLE public.employees (
      id integer NOT NULL,
      vacation_hours smallint NOT NULL,
      CONSTRAINT employees_check CHECK (vacation_hours >= '-40'::integer AND vacation_hours <= 240)
  );
desired: |
  CREATE TABLE public.employees (
      id integer NOT NULL,
      vacation_hours smallint NOT NULL,
      CHECK (vacation_hours >= -40 AND vacation_hours <= 240)
  );
applied: |
  -- public.employees
  CREATE TABLE public.employees (
      id integer NOT NULL,
      vacation_hours smallint NOT NULL,
      CONSTRAINT employees_check CHECK (vacation_hours >= '-40'::integer AND vacation_hours <= 240)
  );
applied_sql: ""
verify_no_drift: true
