bulk_alter: true
init: |
  CREATE TABLE public.items (
      id integer GENERATED ALWAYS AS IDENTITY (START WITH 100 CACHE 20),
      code integer NOT NULL
  );
desired: |
  CREATE TABLE public.items (
      id integer GENERATED BY DEFAULT AS IDENTITY (INCREMENT BY 5),
      code integer GENERATED ALWAYS AS IDENTITY (START WITH 50)
  );
applied: |
  -- public.items
  CREATE TABLE public.items (
      id integer GENERATED BY DEFAULT AS IDENTITY (INCREMENT BY 5),
      code integer GENERATED ALWAYS AS IDENTITY (START WITH 50)
  );
applied_sql: |
  ALTER TABLE public.items
    ALTER COLUMN id SET GENERATED BY DEFAULT SET START WITH 1 SET INCREMENT BY 5 SET CACHE 1,
    ALTER COLUMN code ADD GENERATED ALWAYS AS IDENTITY (START WITH 50);
