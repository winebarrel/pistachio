init: |
  CREATE TABLE public.items (
      id integer GENERATED ALWAYS AS IDENTITY (START WITH 100 INCREMENT BY 5)
  );
desired: |
  CREATE TABLE public.items (
      id integer GENERATED ALWAYS AS IDENTITY (START WITH 200 INCREMENT BY 5 MAXVALUE 900 CACHE 20 CYCLE)
  );
applied: |
  -- public.items
  CREATE TABLE public.items (
      id integer GENERATED ALWAYS AS IDENTITY (START WITH 200 INCREMENT BY 5 MAXVALUE 900 CACHE 20 CYCLE)
  );
