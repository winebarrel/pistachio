init: |
  CREATE TABLE public."MyTable" (
      "Id" integer NOT NULL,
      CONSTRAINT "MyTable_pkey" PRIMARY KEY ("Id")
  );
desired: |
  CREATE TABLE public."MyTable" (
      "Id" integer NOT NULL,
      "Name" text NOT NULL,
      CONSTRAINT "MyTable_pkey" PRIMARY KEY ("Id")
  );
applied: |
  -- public."MyTable"
  CREATE TABLE public."MyTable" (
      "Id" integer NOT NULL,
      "Name" text NOT NULL,
      CONSTRAINT "MyTable_pkey" PRIMARY KEY ("Id")
  );
