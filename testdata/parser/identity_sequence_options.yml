input: |
  CREATE TABLE public.events (
      id integer GENERATED BY DEFAULT AS IDENTITY (START WITH 100 INCREMENT BY 5 MINVALUE 10 MAXVALUE 1000 CACHE 20 CYCLE),
      seq bigint GENERATED ALWAYS AS IDENTITY (INCREMENT BY -1),
      plain smallint GENERATED ALWAYS AS IDENTITY (START WITH 1 CACHE 1)
  );
expected: |
  -- public.events
  CREATE TABLE public.events (
      id integer GENERATED BY DEFAULT AS IDENTITY (START WITH 100 INCREMENT BY 5 MINVALUE 10 MAXVALUE 1000 CACHE 20 CYCLE),
      seq bigint GENERATED ALWAYS AS IDENTITY (INCREMENT BY -1),
      plain smallint GENERATED ALWAYS AS IDENTITY
  );
