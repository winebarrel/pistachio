input: |
  CREATE TABLE myschema.users (
      id integer NOT NULL,
      name text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE INDEX ON myschema.users (name);
expected: |
  -- myschema.users
  CREATE TABLE myschema.users (
      id integer NOT NULL,
      name text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE INDEX users_name_idx ON myschema.users USING btree (name);
