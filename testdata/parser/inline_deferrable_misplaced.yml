input: |
  CREATE TABLE public.t (
      a integer DEFERRABLE,
      b integer NOT NULL INITIALLY DEFERRED
  );
expected: |
  -- public.t
  CREATE TABLE public.t (
      a integer,
      b integer NOT NULL
  );
