input: |
  CREATE TYPE public.status AS ENUM ('active','inactive','pending');
expected: |
  -- public.status
  CREATE TYPE public.status AS ENUM (
      'active',
      'inactive',
      'pending'
  );
