explain: true
init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      name text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  INSERT INTO public.users SELECT g, 'n' FROM generate_series(1, 3) g;
  ANALYZE public.users;
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      name text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE MATERIALIZED VIEW public.user_count AS SELECT count(*) AS cnt FROM users;
  CREATE MATERIALIZED VIEW public.one AS SELECT 1 AS n;
plan: |
  CREATE MATERIALIZED VIEW public.one AS
  SELECT 1 AS n;
  -- scan, blocks nothing: public.users (~3 rows, 8192 bytes)
  CREATE MATERIALIZED VIEW public.user_count AS
  SELECT count(*) AS cnt FROM users;
