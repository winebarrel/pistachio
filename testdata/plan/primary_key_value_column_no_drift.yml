init: |
  CREATE TABLE public.settings (
      value text NOT NULL,
      CONSTRAINT settings_pkey PRIMARY KEY (value)
  );
desired: |
  CREATE TABLE public.settings (
      value text NOT NULL,
      CONSTRAINT settings_pkey PRIMARY KEY (value)
  );
plan: ""
