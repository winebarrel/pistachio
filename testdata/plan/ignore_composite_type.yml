init: |
  CREATE TYPE public.address AS (street text);
desired: |
  -- pista:ignore
  CREATE TYPE public.address AS (
      street text,
      city   text
  );
plan: ""
ignored: |
  -- ignored: public.address
