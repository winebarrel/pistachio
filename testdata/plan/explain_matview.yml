explain: true
init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      name text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  INSERT INTO public.users SELECT g, 'n' FROM generate_series(1, 3) g;
  ANALYZE public.users;
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      name text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE MATERIALIZED VIEW public.user_stats AS SELECT count(*) AS cnt FROM public.users;
  CREATE VIEW public.user_names AS SELECT name FROM public.users;
plan: |
  CREATE OR REPLACE VIEW public.user_names AS
  SELECT name FROM public.users;
  -- scan, blocks nothing: public.users (~3 rows, 8192 bytes)
  CREATE MATERIALIZED VIEW public.user_stats AS
  SELECT count(*) AS cnt FROM public.users;
