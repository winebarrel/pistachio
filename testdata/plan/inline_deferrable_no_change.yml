init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      code text,
      CONSTRAINT users_code_key UNIQUE (code) DEFERRABLE,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE TABLE public.posts (
      id integer NOT NULL,
      user_id integer,
      CONSTRAINT posts_pkey PRIMARY KEY (id)
  );
  ALTER TABLE ONLY public.posts ADD CONSTRAINT posts_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users (id) DEFERRABLE INITIALLY DEFERRED;
desired: |
  CREATE TABLE public.users (
      id integer PRIMARY KEY,
      code text UNIQUE DEFERRABLE
  );
  CREATE TABLE public.posts (
      id integer PRIMARY KEY,
      user_id integer REFERENCES public.users (id) DEFERRABLE INITIALLY DEFERRED
  );
plan: ""
