init: ""
desired: |
  CREATE TYPE public.address AS (
      street text,
      city   text
  );
plan: |
  CREATE TYPE public.address AS (
      street text,
      city text
  );
count:
  composite_types: 0
