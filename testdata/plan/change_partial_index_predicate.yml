init: |
  CREATE TABLE public.products (
      id integer NOT NULL,
      qty integer NOT NULL,
      CONSTRAINT products_pkey PRIMARY KEY (id)
  );
  CREATE INDEX idx_products_qty ON public.products (qty) WHERE qty > 0;
desired: |
  CREATE TABLE public.products (
      id integer NOT NULL,
      qty integer NOT NULL,
      CONSTRAINT products_pkey PRIMARY KEY (id)
  );
  CREATE INDEX idx_products_qty ON public.products (qty) WHERE qty > 100;
plan: |
  DROP INDEX public.idx_products_qty;
  CREATE INDEX idx_products_qty ON public.products USING btree (qty) WHERE qty > 100;
