explain: true
manage_storage_param: true
init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      name text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE VIEW public.user_names AS SELECT name FROM public.users;
  CREATE MATERIALIZED VIEW public.user_count AS SELECT count(*) AS cnt FROM public.users;
  INSERT INTO public.users SELECT g, 'n' FROM generate_series(1, 3) g;
  ANALYZE public.users;
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      name text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE VIEW public.user_names WITH (security_barrier = true) AS SELECT name FROM public.users;
  CREATE MATERIALIZED VIEW public.user_count WITH (fillfactor = 70) AS SELECT count(*) AS cnt FROM public.users;
plan: |
  ALTER MATERIALIZED VIEW public.user_count SET (fillfactor='70');
  ALTER VIEW public.user_names SET (security_barrier='true');
