# ADD COLUMN cannot carry either setting on 15, so the column arrives plain and
# the settings follow it.
init: |
  CREATE TABLE public.docs (
      id integer NOT NULL,
      CONSTRAINT docs_pkey PRIMARY KEY (id)
  );
desired: |
  CREATE TABLE public.docs (
      id integer NOT NULL,
      body text STORAGE main COMPRESSION pglz,
      CONSTRAINT docs_pkey PRIMARY KEY (id)
  );
plan: |
  ALTER TABLE public.docs ADD COLUMN body text;
  ALTER TABLE public.docs ALTER COLUMN body SET STORAGE MAIN;
  ALTER TABLE public.docs ALTER COLUMN body SET COMPRESSION pglz;
