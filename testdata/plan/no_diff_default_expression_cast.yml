# The catalog keeps a cast on an expression that changes something, and drops
# one that changes nothing. It spells timestamptz(0) as
# timestamp(0) with time zone.
init: |
  CREATE TABLE public.t (
      a timestamp with time zone DEFAULT now()::timestamp(0) with time zone,
      b timestamp with time zone DEFAULT now()::timestamptz,
      c date DEFAULT now()::date,
      d timestamp with time zone DEFAULT now()::timestamptz(0),
      e timestamp with time zone DEFAULT now()::timestamp(0),
      f text[] DEFAULT ARRAY[]::text[],
      g date DEFAULT CURRENT_TIMESTAMP::date,
      h date DEFAULT ('now'::text)::date,
      i text DEFAULT gen_random_uuid()::text,
      j bigint DEFAULT (extract(epoch from now()) * 1000)::bigint,
      k timestamp DEFAULT timezone('utc', now())::timestamp(0)
  );
desired: |
  CREATE TABLE public.t (
      a timestamp with time zone DEFAULT now()::timestamp(0) with time zone,
      b timestamp with time zone DEFAULT now()::timestamptz,
      c date DEFAULT now()::date,
      d timestamp with time zone DEFAULT now()::timestamptz(0),
      e timestamp with time zone DEFAULT now()::timestamp(0),
      f text[] DEFAULT ARRAY[]::text[],
      g date DEFAULT CURRENT_TIMESTAMP::date,
      h date DEFAULT ('now'::text)::date,
      i text DEFAULT gen_random_uuid()::text,
      j bigint DEFAULT (extract(epoch from now()) * 1000)::bigint,
      k timestamp DEFAULT timezone('utc', now())::timestamp(0)
  );
plan: ""
