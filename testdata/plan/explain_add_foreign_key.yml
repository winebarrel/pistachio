explain: true
init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE TABLE public.posts (
      id integer NOT NULL,
      user_id integer NOT NULL,
      editor_id integer,
      CONSTRAINT posts_pkey PRIMARY KEY (id)
  );
  INSERT INTO public.users SELECT g FROM generate_series(1, 3) g;
  INSERT INTO public.posts VALUES (1, 1, NULL), (2, 2, NULL);
  ANALYZE public.users;
  ANALYZE public.posts;
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE TABLE public.posts (
      id integer NOT NULL,
      user_id integer NOT NULL,
      editor_id integer,
      CONSTRAINT posts_pkey PRIMARY KEY (id),
      CONSTRAINT posts_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users (id),
      CONSTRAINT posts_editor_id_fkey FOREIGN KEY (editor_id) REFERENCES public.users (id) NOT VALID
  );
plan: |
  -- scan, blocks writes: public.posts (~2 rows, 8192 bytes, as of {{today}}), public.users (~3 rows, 8192 bytes, as of {{today}})
  ALTER TABLE ONLY public.posts ADD CONSTRAINT posts_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users (id);
  ALTER TABLE ONLY public.posts ADD CONSTRAINT posts_editor_id_fkey FOREIGN KEY (editor_id) REFERENCES public.users (id) NOT VALID;
