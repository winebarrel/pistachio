init: |
  CREATE TABLE public.parent (id integer);
  CREATE TABLE public.child (extra text) INHERITS (public.parent);
desired: |
  CREATE TABLE public.parent (
      id integer,
      n integer,
      CONSTRAINT parent_n_check CHECK (n > 0)
  );
  CREATE TABLE public.child (
      extra text
  )
  INHERITS (public.parent);
plan: |
  ALTER TABLE public.parent ADD COLUMN n integer;
  ALTER TABLE public.parent ADD CONSTRAINT parent_n_check CHECK (n > 0);
