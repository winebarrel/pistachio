# PostgreSQL refuses to leave a logged table referencing an unlogged one, so
# the referencing table has to turn unlogged first. The desired file lists the
# referenced table first, which is the order that fails.
init: |
  CREATE TABLE public.parent (
      id integer NOT NULL,
      CONSTRAINT parent_pkey PRIMARY KEY (id)
  );
  CREATE TABLE public.child (
      id integer NOT NULL,
      p integer,
      CONSTRAINT child_pkey PRIMARY KEY (id),
      CONSTRAINT child_p_fkey FOREIGN KEY (p) REFERENCES public.parent(id)
  );
desired: |
  CREATE UNLOGGED TABLE public.parent (
      id integer NOT NULL,
      CONSTRAINT parent_pkey PRIMARY KEY (id)
  );
  CREATE UNLOGGED TABLE public.child (
      id integer NOT NULL,
      p integer,
      CONSTRAINT child_pkey PRIMARY KEY (id),
      CONSTRAINT child_p_fkey FOREIGN KEY (p) REFERENCES public.parent(id)
  );
plan: |
  ALTER TABLE public.child SET UNLOGGED;
  ALTER TABLE public.parent SET UNLOGGED;
