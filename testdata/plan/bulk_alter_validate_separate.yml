bulk_alter: true
init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      age integer,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  ALTER TABLE public.users ADD CONSTRAINT users_age_pos CHECK (age > 0) NOT VALID;
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      age integer,
      email text,
      score integer,
      CONSTRAINT users_pkey PRIMARY KEY (id),
      CONSTRAINT users_age_pos CHECK (age > 0)
  );
plan: |
  ALTER TABLE public.users
    ADD COLUMN email text,
    ADD COLUMN score integer;
  ALTER TABLE public.users VALIDATE CONSTRAINT users_age_pos;
