# The same fold on an enum column, where the catalog casts the value to the
# enum type rather than to text.
init: |
  CREATE TYPE public.parcel_state AS ENUM ('paid', 'shipped', 'canceled');
  CREATE TABLE public.parcels (
      id integer NOT NULL,
      state public.parcel_state,
      CONSTRAINT parcels_pkey PRIMARY KEY (id),
      CONSTRAINT parcels_state_check CHECK (state IN ('paid')),
      CONSTRAINT parcels_state_open CHECK (state NOT IN ('canceled'))
  );
desired: |
  CREATE TYPE public.parcel_state AS ENUM ('paid', 'shipped', 'canceled');
  CREATE TABLE public.parcels (
      id integer NOT NULL,
      state public.parcel_state,
      CONSTRAINT parcels_pkey PRIMARY KEY (id),
      CONSTRAINT parcels_state_check CHECK (state IN ('paid')),
      CONSTRAINT parcels_state_open CHECK (state NOT IN ('canceled'))
  );
plan: ""
