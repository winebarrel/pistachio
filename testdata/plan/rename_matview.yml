init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE MATERIALIZED VIEW public.user_stats AS SELECT count(*) AS cnt FROM public.users;
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  -- pist:renamed-from public.user_stats
  CREATE MATERIALIZED VIEW public.user_counts AS SELECT count(*) AS cnt FROM public.users;
plan: |
  ALTER MATERIALIZED VIEW public.user_stats RENAME TO user_counts;
