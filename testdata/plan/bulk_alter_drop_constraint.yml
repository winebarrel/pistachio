bulk_alter: true
init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      age integer,
      CONSTRAINT users_pkey PRIMARY KEY (id),
      CONSTRAINT users_age_pos CHECK (age > 0),
      CONSTRAINT users_age_lt100 CHECK (age < 100)
  );
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      age integer,
      email text,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
plan: |
  ALTER TABLE public.users
    ADD COLUMN email text,
    DROP CONSTRAINT users_age_lt100,
    DROP CONSTRAINT users_age_pos;
