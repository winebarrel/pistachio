explain: true
init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      amount integer,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  INSERT INTO public.users SELECT g, g FROM generate_series(1, 3) g;
  ANALYZE public.users;
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      -- pista:renamed-from amount
      total bigint,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
plan: |
  ALTER TABLE public.users RENAME COLUMN amount TO total;
  -- rewrite, blocks reads and writes: public.users (~3 rows, 8192 bytes, as of {{today}}, 1 index rebuilt)
  ALTER TABLE public.users ALTER COLUMN total SET DATA TYPE bigint;
