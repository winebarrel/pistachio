explain: true
init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      note text,
      code character varying(100),
      amount numeric(12,2),
      tags text,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  INSERT INTO public.users SELECT g, 'n', 'c', 1.5, 't' FROM generate_series(1, 3) g;
  ANALYZE public.users;
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      note character varying(20),
      code character varying(50),
      amount numeric(12,3),
      tags text[],
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
plan: |
  -- rewrite, blocks reads and writes: public.users (~3 rows, 8192 bytes, as of {{today}}, 1 index rebuilt)
  ALTER TABLE public.users ALTER COLUMN note SET DATA TYPE character varying(20);
  -- rewrite, blocks reads and writes: public.users (~3 rows, 8192 bytes, as of {{today}}, 1 index rebuilt)
  ALTER TABLE public.users ALTER COLUMN code SET DATA TYPE character varying(50);
  -- rewrite, blocks reads and writes: public.users (~3 rows, 8192 bytes, as of {{today}}, 1 index rebuilt)
  ALTER TABLE public.users ALTER COLUMN amount SET DATA TYPE numeric(12,3);
  -- rewrite, blocks reads and writes: public.users (~3 rows, 8192 bytes, as of {{today}}, 1 index rebuilt)
  ALTER TABLE public.users ALTER COLUMN tags SET DATA TYPE text[];
