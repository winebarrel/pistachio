explain: true
init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      note text,
      amount numeric(10,2),
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  INSERT INTO public.users SELECT g, 'n', 1.5 FROM generate_series(1, 3) g;
  ANALYZE public.users;
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      note character varying(20),
      amount numeric(12,2),
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
plan: |
  -- rewrite, blocks reads and writes: public.users (~3 rows, 8192 bytes, 1 index rebuilt)
  ALTER TABLE public.users ALTER COLUMN note SET DATA TYPE character varying(20);
  ALTER TABLE public.users ALTER COLUMN amount SET DATA TYPE numeric(12,2);
