explain: true
init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      name text,
      note text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  INSERT INTO public.users SELECT g, 'n', 'x' FROM generate_series(1, 3) g;
  ANALYZE public.users;
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      name text NOT NULL,
      note text,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
plan: |
  -- scan, blocks reads and writes: public.users (~3 rows, 8192 bytes, as of {{today}})
  ALTER TABLE public.users ALTER COLUMN name SET NOT NULL;
  ALTER TABLE public.users ALTER COLUMN note DROP NOT NULL;
