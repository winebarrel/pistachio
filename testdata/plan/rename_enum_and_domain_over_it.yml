init: |
  CREATE TYPE public.status AS ENUM ('active', 'inactive');
  CREATE DOMAIN public.status_domain AS public.status;
  CREATE TABLE public.users (
      id integer NOT NULL,
      status public.status_domain,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
desired: |
  -- pista:renamed-from public.status
  CREATE TYPE public.user_status AS ENUM ('active', 'inactive');
  -- pista:renamed-from public.status_domain
  CREATE DOMAIN public.user_status_domain AS public.user_status;
  CREATE TABLE public.users (
      id integer NOT NULL,
      status public.user_status_domain,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
plan: |
  ALTER TYPE public.status RENAME TO user_status;
  ALTER DOMAIN public.status_domain RENAME TO user_status_domain;
