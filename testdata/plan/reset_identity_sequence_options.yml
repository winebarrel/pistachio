init: |
  CREATE TABLE public.items (
      id integer GENERATED ALWAYS AS IDENTITY (START WITH 100 INCREMENT BY 5 CACHE 20 CYCLE)
  );
desired: |
  CREATE TABLE public.items (
      id integer GENERATED ALWAYS AS IDENTITY
  );
plan: |
  ALTER TABLE public.items ALTER COLUMN id SET START WITH 1 SET INCREMENT BY 1 SET CACHE 1 SET NO CYCLE;
