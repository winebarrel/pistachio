explain: true
init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  INSERT INTO public.users SELECT g FROM generate_series(1, 3) g;
  ANALYZE public.users;
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      a integer,
      b integer NOT NULL DEFAULT 0,
      c timestamp with time zone DEFAULT now(),
      d uuid DEFAULT gen_random_uuid(),
      e integer NOT NULL,
      f bigserial NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
plan: |
  ALTER TABLE public.users ADD COLUMN a integer;
  ALTER TABLE public.users ADD COLUMN b integer DEFAULT 0 NOT NULL;
  ALTER TABLE public.users ADD COLUMN c timestamp with time zone DEFAULT now();
  -- rewrite, blocks reads and writes: public.users (~3 rows, 8192 bytes, as of {{today}}, 1 index rebuilt)
  ALTER TABLE public.users ADD COLUMN d uuid DEFAULT gen_random_uuid();
  -- scan, blocks reads and writes: public.users (~3 rows, 8192 bytes, as of {{today}})
  ALTER TABLE public.users ADD COLUMN e integer NOT NULL;
  -- rewrite, blocks reads and writes: public.users (~3 rows, 8192 bytes, as of {{today}}, 1 index rebuilt)
  ALTER TABLE public.users ADD COLUMN f bigserial NOT NULL;
