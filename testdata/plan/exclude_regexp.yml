exclude: ['/^(tmp|scratch)_/']
init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE TABLE public.tmp_backup (
      id integer NOT NULL,
      CONSTRAINT tmp_backup_pkey PRIMARY KEY (id)
  );
desired: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      name text,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE TABLE public.tmp_backup (
      id integer NOT NULL,
      name text,
      CONSTRAINT tmp_backup_pkey PRIMARY KEY (id)
  );
plan: |
  ALTER TABLE public.users ADD COLUMN name text;
