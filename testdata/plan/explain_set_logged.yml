explain: true
init: |
  CREATE UNLOGGED TABLE public.sessions (
      id integer NOT NULL,
      token text NOT NULL,
      CONSTRAINT sessions_pkey PRIMARY KEY (id)
  );
  INSERT INTO public.sessions SELECT g, 't' FROM generate_series(1, 3) g;
  ANALYZE public.sessions;
desired: |
  CREATE TABLE public.sessions (
      id integer NOT NULL,
      token text NOT NULL,
      CONSTRAINT sessions_pkey PRIMARY KEY (id)
  );
plan: |
  -- rewrite, blocks reads and writes: public.sessions (~3 rows, 8192 bytes, as of {{today}}, 1 index rebuilt)
  ALTER TABLE public.sessions SET LOGGED;
