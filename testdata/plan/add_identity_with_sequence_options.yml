init: |
  CREATE TABLE public.items (
      id integer NOT NULL
  );
desired: |
  CREATE TABLE public.items (
      id integer GENERATED ALWAYS AS IDENTITY (START WITH 50 INCREMENT BY 2 CACHE 10)
  );
plan: |
  ALTER TABLE public.items ALTER COLUMN id ADD GENERATED ALWAYS AS IDENTITY (START WITH 50 INCREMENT BY 2 CACHE 10);
