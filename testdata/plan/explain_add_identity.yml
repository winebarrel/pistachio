explain: true
init: |
  CREATE TABLE public.items (
      id integer NOT NULL,
      name text NOT NULL,
      CONSTRAINT items_pkey PRIMARY KEY (id)
  );
  INSERT INTO public.items SELECT g, 'n' FROM generate_series(1, 3) g;
  ANALYZE public.items;
desired: |
  CREATE TABLE public.items (
      id integer GENERATED ALWAYS AS IDENTITY,
      name text NOT NULL,
      seq integer GENERATED BY DEFAULT AS IDENTITY,
      CONSTRAINT items_pkey PRIMARY KEY (id)
  );
plan: |
  -- rewrite, blocks reads and writes: public.items (~3 rows, 8192 bytes, as of {{today}}, 1 index rebuilt)
  ALTER TABLE public.items ADD COLUMN seq integer GENERATED BY DEFAULT AS IDENTITY;
  ALTER TABLE public.items ALTER COLUMN id ADD GENERATED ALWAYS AS IDENTITY;
