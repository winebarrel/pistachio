init: |
  CREATE TABLE public.documents (
      id integer NOT NULL,
      data jsonb NOT NULL,
      metadata json,
      CONSTRAINT documents_pkey PRIMARY KEY (id)
  );
dump: |
  -- public.documents
  CREATE TABLE public.documents (
      id integer NOT NULL,
      data jsonb NOT NULL,
      metadata json,
      CONSTRAINT documents_pkey PRIMARY KEY (id)
  );
