init: |
  CREATE TABLE public.users (
      id serial PRIMARY KEY,
      name text NOT NULL
  );
dump: |
  -- public.users
  CREATE TABLE public.users (
      id serial NOT NULL,
      name text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
