sort_by_deps: true
init: |
  CREATE TABLE public.text (
      id integer NOT NULL,
      body text,
      tags text[],
      CONSTRAINT text_pkey PRIMARY KEY (id)
  );
dump: |
  -- public.text
  CREATE TABLE public.text (
      id integer NOT NULL,
      body text,
      tags text[],
      CONSTRAINT text_pkey PRIMARY KEY (id)
  );
