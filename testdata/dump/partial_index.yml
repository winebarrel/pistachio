init: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      active boolean DEFAULT true NOT NULL,
      name text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE INDEX idx_users_active ON public.users USING btree (name) WHERE active;
dump: |
  -- public.users
  CREATE TABLE public.users (
      id integer NOT NULL,
      active boolean DEFAULT true NOT NULL,
      name text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
  CREATE INDEX idx_users_active ON public.users USING btree (name) WHERE active;
