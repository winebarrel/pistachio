drop_policy:
  allow_drop: []
current: |
  CREATE TABLE public.users (
      id integer NOT NULL,
      name text NOT NULL,
      CONSTRAINT users_pkey PRIMARY KEY (id)
  );
desired: ""
diff: ""
disallowed_drops: |
  -- skipped: DROP TABLE public.users;
